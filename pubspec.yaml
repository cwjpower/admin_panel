<<<<<<< HEAD
name: admin_panel
description: HeroComics Admin Panel
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  file_picker: ^8.0.0  # ⭐ NEW!

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
=======
name: herocomics_app
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.1.0                    # 이 줄이 있는지 확인
  shared_preferences: ^2.2.2      # 이 줄이 있는지 확인

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
>>>>>>> 9084b6b (chore: initial commit (herocomics_app))
  uses-material-design: true